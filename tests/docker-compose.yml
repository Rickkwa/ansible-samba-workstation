version: '3'

services:
  centos7:
    build:
      context: ./environments
      dockerfile: centos-7.Dockerfile
    privileged: true
    ports:
      - "2200:22"

  ubuntu16:
    build:
      context: ./environments
      dockerfile: ubuntu.Dockerfile
    ports:
      - "2201:22"
